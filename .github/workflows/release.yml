# This is a basic workflow to help you get started with Actions

name: Create New Release and Upload Binaries

on:
  workflow_dispatch:
  push:
    tags:
      - "v*.*.*"

# softprops/action-gh-release needs write access to create the release and
# upload binaries. Without this the job builds fine but fails the upload with
# a 403 ("Resource not accessible by integration").
permissions:
  contents: write

jobs:
  build:
    name: Upload Release Asset
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - uses: actions/setup-go@v2
        with:
          go-version: "^1.26.4"
      - run: go version

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 22

      - name: Install Task
        uses: arduino/setup-task@v1

      - name: Build
        run: task release:build

      - name: Create Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/v')
        with:
          draft: true
          prerelease: false
          fail_on_unmatched_files: true
          name: Release ${{ github.ref }}
          files: |
            ./build/*
